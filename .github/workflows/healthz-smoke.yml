name: Healthz smoke (in-process)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_  workflow_  workflow_  workflow_  wo Backend healthz smoke
    runs-on    runs-on    runs-on    runs-on   run:
        working-directory: idsideAI_FIXED
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5      - uses: actions/setup-python@v5      - uses: actions/setup-python@v5      - uses: actions/setup-python@v5      - uses: actions/setup-python@v5      - uses: actions/setup-python@v5      - uses: actions/setup-python@v5      - uses: actions/setup-python@v5      - uses: actions/setup-python@v5      - uses: actions/setup-python@v5      - uses: actions/setup-python@v5      - uses: actions/setupimport Test      - uses: actions/setup-python@v5      - uses: actions/setup-python@v5      - uses: actions/setup-python@v5   nt("body:", r.text)
assert r.status_code == 200, f"bad status {r.statassert r.status_codk" in r.text.lower(), "missing 'ok' in body"
open("healthz.txt","w").write(r.text)
PY

      - name: Upload healthz artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-healthz
                                                      f-no-files-found: warn
